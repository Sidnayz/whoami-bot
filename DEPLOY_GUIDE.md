# 🚀 Подробная инструкция по деплою на Render

## 📋 Оглавление
1. [Подготовка](#1-подготовка)
2. [Создание аккаунта и подключение](#2-создание-аккаунта)
3. [Настройка деплоя](#3-настройка-деплоя)
4. [Первый деплой](#4-первый-деплой)
5. [Проверка работы](#5-проверка-работы)
6. [Управление и логи](#6-управление-и-логи)
7. [Решение проблем](#7-решение-проблем)

---

## 1. Подготовка

### 1.1 Проверьте репозиторий
Убедитесь, что все файлы на GitHub:
```bash
# Посмотреть список файлов
git ls

# Убедитесь, что Dockerfile есть
git show HEAD:Dockerfile
```

**Ваш репозиторий должен содержать:**
- ✅ `main.py`
- ✅ `requirements.txt`
- ✅ `Dockerfile` (для деплоя с Docker)
- ✅ `.dockerignore`
- ✅ `.env.example`
- ✅ `README.md`

### 1.2 Получите токен бота
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например: `whoami_game_bot`)
   - Описание (по желанию)
   - Настройте: /start - запуск, /help - справка
5. **Сохраните токен** (начинается с `123456789:ABC...`)

⚠️ **ВАЖНО:** Не публикуйте токен и не добавляйте его в Git!

---

## 2. Создание аккаунта и подключение

### 2.1 Регистрация на Render
1. Перейдите на [https://dashboard.render.com](https://dashboard.render.com)
2. Нажмите кнопку **"Sign Up"** (если нет аккаунта)
3. Регистрация:
   - Email (лучше использовать Gmail)
   - Пароль (сохраните!)
   - Имя пользователя
4. Подтвердите email через письмо

### 2.2 Подключение GitHub (один раз)
1. После регистрации вы окажетесь в dashboard
2. Нажмите **"New +"** в правом верхнем углу
3. Выберите **"Web Service"**
4. Нажмите **"Connect GitHub"**
5. Авторизуйтесь в GitHub (если потребуется)
6. Дайте доступ Render к вашему аккаунту
7. Выберите репозиторий: `Sidnayz/whoami-bot`

---

## 3. Настройка деплоя

### 3.1 Создание Web Service

1. После подключения GitHub вы увидите список ваших репозиториев
2. Найдите `Sidnayz/whoami-bot`
3. Нажмите **"New +"** на карточке репозитория
4. Откроется форма создания сервиса

### 3.2 Основные настройки

#### General Settings:

| Параметр | Значение | Описание |
|-----------|----------|-----------|
| **Name** | `whoami-bot` | Имя сервиса (любое) |
| **Region** | `Oregon (us-west)` | Ближайший регион к РФ |
| **Branch** | `main` | Ветка Git (по умолчанию) |
| **Environment** | `Docker` ⭐ | Использовать Dockerfile |
| **Root Directory** | `/` | Корневая директория |

⚠️ **ВАЖНО:** Убедитесь, что **Environment** установлен в **"Docker"**!

#### Build Settings:

| Параметр | Значение |
|-----------|----------|
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python main.py` |

💡 **Примечание:** Render автоматически определит Dockerfile, если Environment установлен в Docker.

### 3.3 Environment Variables (ОБЯЗАТЕЛЬНО!)

1. Прокрутите вниз до раздела **"Advanced"**
2. Нажмите **"Add Environment Variable"**

Добавьте следующую переменную:

| Key | Value | Обязательно? |
|-----|-------|--------------|
| `BOT_TOKEN` | `123456789:ABCdefGHIjkLmnoPQRstu` | ✅ ДА |

**Где взять токен:**
- Скопируйте токен, который вы получили от @BotFather
- Вставьте в поле Value
- Нажмите **"Save"**

⚠️ **ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ:**
- ❌ **НЕ** добавляйте `.env` файл в Git!
- ❌ **НЕ** коммить `BOT_TOKEN` в код!
- ❌ Токен используется ТОЛЬКО через Environment Variables на Render!

### 3.4 Дополнительные настройки (опционально)

**Plans:** оставьте `Free`
**Concurrency:** `1` (для бесплатного тарифа)
**Auto-Deploys:** `OFF` (не автоматический деплой при пуше)

### 3.5 Финальная проверка перед деплоем

Проверьте настройки:
```
✅ Environment = Docker
✅ Build Command = pip install -r requirements.txt
✅ Start Command = python main.py
✅ BOT_TOKEN добавлен
✅ Region выбран
```

---

## 4. Первый деплой

### 4.1 Запуск деплоя

1. Внизу страницы нажмите **"Create Web Service"**
2. Render начнёт сборку Docker образа
3. Это займёт 2-5 минут
4. Сначала будет **"Build in progress"**
5. Потом **"Deploying"**

### 4.2 Что происходит во время деплоя

Render выполняет следующие действия:

1. **Clone repository**
   ```
   Cloning from https://github.com/Sidnayz/whoami-bot
   ==> Checking out commit ...
   ```

2. **Build Docker image**
   ```
   Sending build context to Docker daemon...
   Step 1/6: FROM python:3.12-slim
   Step 2/6: WORKDIR /app
   Step 3/6: COPY requirements.txt .
   Step 4/6: RUN pip install --no-cache-dir -r requirements.txt
   Step 5/6: COPY . .
   Step 6/6: CMD ["python", "main.py"]
   ```

3. **Deploy container**
   ```
   Deploying...
   Restarting service...
   ```

### 4.3 Успешный деплой

Когда деплой завершится успешно:

✅ **Статус:** "Live"
✅ **Domain:** Вы увидите URL вида: `https://whoami-bot.onrender.com`
✅ **Служба запущена** (зелёный индикатор)

---

## 5. Проверка работы

### 5.1 Получение URL бота

После успешного деплоя Render покажет:
- URL сервиса: `https://whoami-bot.onrender.com` (или другой домен)
- Этот URL - это webhook URL для Telegram

### 5.2 Настройка Webhook (автоматически)

Render автоматически настраивает webhook для вашего бота! 🎉

**Проверьте webhook:**
1. Перейдите в [@BotFather](https://t.me/BotFather)
2. Выберите своего бота
3. Перейдите в **"Webhook Info"**
4. Вы должны увидеть URL: `https://whoami-bot.onrender.com`
5. Если URL установлен - всё отлично!

### 5.3 Тестирование бота

1. Найдите бота в Telegram по имени
2. Нажмите **"Start"** или отправьте `/start`
3. Бот должен ответить справкой
4. Добавьте бота в групповой чат
5. Напишите `/startgame` для начала игры

### 5.4 Проверка логов

1. На странице сервиса нажмите **"Logs"**
2. Вы должны видеть:
   ```
   Application is starting...
   Bot polling started...
   ```
3. Нет красных ERROR сообщений

---

## 6. Управление и логи

### 6.1 Мониторинг

На главной странице Render:
- 🟢 **Live** - сервис работает
- 🟡 **Build failed** - ошибка сборки
- 🔴 **Deploy failed** - ошибка деплоя
- 🟣 **Degraded** - проблемы с производительностью

### 6.2 Просмотр логов

1. Откройте сервис → **"Logs"**
2. Выберите вкладку:
   - **Real-time** - текущие логи
   - **Past** - исторические логи

3. Типы сообщений:
   - `INFO` - информационные (нормально)
   - `WARNING` - предупреждения
   - `ERROR` - ошибки (красные)

### 6.3 Перезапуск сервиса

Если бот работает неправильно:

**Вариант 1: Manual Deploy**
1. На странице сервиса нажмите **"Manual Deploy"**
2. Render пересоберёт и задеплоит снова

**Вариант 2: Restart**
1. На странице сервиса нажмите три точки `⋮` справа от названия
2. Выберите **"Restart service"**

**Вариант 3: Изменение переменных**
1. Перейдите в **"Environment"**
2. Измените `BOT_TOKEN` и нажмите "Save"
3. Сервис автоматически перезапустится

---

## 7. Решение проблем

### 7.1 Общие ошибки деплоя

#### Ошибка: "Build failed"

**Причины:**
- Синтаксическая ошибка в Dockerfile
- Проблема с зависимостями в requirements.txt
- Ошибка в коде Python

**Решения:**
1. Проверьте Logs → Past для просмотра ошибки
2. Исправьте код локально
3. Сделайте `git commit` и `git push`
4. Нажмите "Manual Deploy"

#### Ошибка: "Deploy failed"

**Причины:**
- BOT_TOKEN не установлен
- Проблемы с webhook
- Бот падает при запуске

**Решения:**
1. Проверьте Environment Variables
2. Убедитесь, что BOT_TOKEN правильный
3. Проверьте Logs → Real-time
4. Сделайте Restart service

#### Ошибка: Бот не отвечает

**Возможные причины:**
1. Webhook не настроен
2. Токен неверный или истёк
3. Бот заблокирован @BotFather
4. Ошибка в коде

**Решения:**
1. Проверьте Webhook Info у BotFather
2. Проверьте, что URL совпадает с Render
3. Получите новый токен
4. Обновите BOT_TOKEN на Render

### 7.2 Ошибки зависимостей

Если видите ошибки при установке пакетов:

```log
ERROR: Could not find a version that satisfies the requirement
```

**Решение:**
1. Убедитесь, что `requirements.txt` содержит:
   ```txt
   aiogram==2.13.1
   aiohttp==3.9.0
   ```
2. Версии должны быть точно такими

### 7.3 Ошибки Docker

Если Docker сборка падает:

```log
ERROR: failed to solve dependency conflicts
```

**Это НЕ ваша проблема!** Render имеет проблемы с pydantic-core.

**Решение:**
- Dockerfile должен решить проблему автоматически
- Используются проверенные версии пакетов
- Если проблема сохраняется - свяжитесь с поддержкой Render

---

## 📝 Чек-лист перед деплоем

Перед деплоем проверьте:

### Код
- [ ] Все файлы на GitHub? (`git status`)
- [ ] Dockerfile существует?
- [ ] requirements.txt правильный?

### На Render
- [ ] Аккаунт создан?
- [ ] GitHub подключён?
- [ ] Bot токен получен?
- [ ] Environment = Docker?
- [ ] BOT_TOKEN добавлен?
- [ ] Build Command настроен?
- [ ] Start Command настроен?

### После деплоя
- [ ] Статус "Live"?
- [ ] URL сервиса получен?
- [ ] Webhook настроен?
- [ ] Бот отвечает на /start?
- [ ] Бот добавлен в группу?

---

## 🎯 Краткая инструкция (для быстрого старта)

1. **Подготовка:**
   - GitHub репозиторий готов
   - Токен бота получен

2. **На Render:**
   - Авторизуйтесь на [dashboard.render.com](https://dashboard.render.com)
   - New + → Web Service → Connect GitHub
   - Выберите `Sidnayz/whoami-bot`

3. **Настройка сервиса:**
   - Name: `whoami-bot`
   - Environment: **Docker** ⭐
   - Region: `Oregon (us-west)`
   - BOT_TOKEN: `ваш_токен`

4. **Деплой:**
   - Нажмите "Create Web Service"
   - Ждите 2-5 минут
   - Статус станет "Live"

5. **Тест:**
   - Найдите бота в Telegram
   - Отправьте `/start`
   - Добавьте в группу
   - Играйте! 🎮

---

## 🔗 Полезные ссылки

- [Render Dashboard](https://dashboard.render.com)
- [Документация Render](https://render.com/docs)
- [Telegram BotFather](https://t.me/BotFather)
- [Документация aiogram](https://docs.aiogram.dev/)

---

## 💡 Советы

1. **Не паникуйте при первом деплое**
   - Потребуется время для сборки Docker
   - Если упадёт - смотрите логи

2. **Используйте описательные имена сервисов**
   - Это поможет идентифицировать в dashboard

3. **Проверьте Logs регулярно**
   - Особенно если бот ведёт себя странно

4. **Сохраните BOT_TOKEN в безопасном месте**
   - Запишите в менеджере паролей
   - Не теряйте токен

5. **Не забудьте про группу в Telegram**
   - После деплоя бот будет работать только в группах
   - Для теста добавьте бота в группу

---

## ❓ Нужна помощь?

Если после выполнения всех шагов:
- ❌ Деплой всё равно падает
- ❌ Бот запускается но не отвечает
- ❌ В логах непонятные ошибки

**Сделайте скриншот:**
- Страницы настроек Render
- Секцию Logs с ошибкой
- Сообщения об ошибке

И покажите - я помогу разобраться! 🤝

---

Удачи в деплое! 🚀
